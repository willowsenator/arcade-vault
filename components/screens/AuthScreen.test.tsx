import { beforeEach, describe, expect, it, vi } from "vitest";
import { fireEvent, render, screen } from "@testing-library/react";
import AuthScreen from "./AuthScreen";
import { AuthProvider } from "@/components/AuthProvider";
const push = vi.fn(); vi.mock("next/navigation", () => ({ useRouter: () => ({ push }) }));
describe("AuthScreen", () => { beforeEach(() => { localStorage.clear(); push.mockClear(); }); it("logs in with the entered username and navigates home", () => { render(<AuthProvider><AuthScreen /></AuthProvider>); fireEvent.change(screen.getByPlaceholderText("px_kai"), { target: { value: "px_kai" } }); fireEvent.click(screen.getByText("ENTRAR AL VAULT")); expect(localStorage.getItem("av_user")).toBe(JSON.stringify({ name: "PX_KAI" })); expect(push).toHaveBeenCalledWith("/"); }); it("plays as guest without setting a user", () => { render(<AuthProvider><AuthScreen /></AuthProvider>); fireEvent.click(screen.getByText("JUGAR COMO INVITADO")); expect(localStorage.getItem("av_user")).toBe("null"); expect(push).toHaveBeenCalledWith("/"); }); it("shows the email field only on the sign-up tab", () => { render(<AuthProvider><AuthScreen /></AuthProvider>); expect(screen.queryByPlaceholderText("jugador@vault.gg")).not.toBeInTheDocument(); fireEvent.click(screen.getByText("CREAR CUENTA")); expect(screen.getByPlaceholderText("jugador@vault.gg")).toBeInTheDocument(); }); });
